applications:
  - name: fitness-buddy
    memory: 512M
    disk_quota: 512M
    instances: 1
    buildpack: python_buildpack
    command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
    env:
      FLASK_DEBUG: "false"
      PYTHON_VERSION: "3.11"
